# No environment variables are required to build or test this site.
# Do not add tokens, service-role keys, or payment secrets here.
#
# Later phases may add server-only variables. They are not configured now.
# Supabase and Stripe are intentionally absent.
